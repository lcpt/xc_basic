//xbusquedabruta.cc

#include "streams/src/imanip.h"

int main(void)
  {
    std::cin >> ignore_to("modelo");
    std::cout << '-' << std::endl << '-' << std::endl;
    vuelca_stream(std::cin,std::cout);
    return 0;
  }
